<%- include('partials/header') %> <%- include('partials/navbar') %>

<div class="container mt-5">
  <h3 class="my-4">Edit Blog 😍</h3>
  <form action="/post/edit/<%= blog._id %> " method="post">
    <div class="form-group">
      <label for="post_title">Blog Title</label>
      <input
        type="text"
        name="title"
        id="post_title"
        class="form-control"
        value="<%= blog.title %> "
        required
      />
    </div>
    <div class="form-group">
      <label for="content">Blog Content</label>
      <textarea
        name="content"
        id="content"
        cols="30"
        rows="10"
        class="form-control"
        required
      >
<%= blog.content %></textarea
      >
    </div>
    <input type="submit" value="Update" class="btn btn-md btn-danger" />
  </form>
</div>
<%- include('partials/footer') %>
